SELECT pages.title FROM pages WHERE (pages.title = "Architecture");
SELECT contributors.username FROM contributors;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "MPF") AND (revisions.timestamp > 1458457622));
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 20;
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1638526092);
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1738786838);
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 15;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1493914097) ORDER BY revisions.timestamp DESC LIMIT 5;
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1718277627) ORDER BY revisions.timestamp ASC;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1511596783);
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1448690438) ORDER BY revisions.timestamp DESC LIMIT 10;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1727648167);
SELECT pages.title FROM pages WHERE (pages.title = "Physique");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "France");
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1387991974);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1758297737);
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "Z484z");
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Robotinator1994") AND (revisions.timestamp > 1576194664));
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 10;
SELECT pages.title FROM pages WHERE (pages.title = "Musique");
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1425882720);
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "AnonymousIP");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id ORDER BY revisions.timestamp DESC LIMIT 5;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1695394697) ORDER BY revisions.timestamp DESC LIMIT 10;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "KhingArthur") AND (revisions.timestamp > 1546393377));
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1706127849) ORDER BY revisions.timestamp DESC LIMIT 8;
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1405383741) ORDER BY revisions.timestamp ASC;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388341269);
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1618666667);
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1742519868);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1387508401);
SELECT contributors.username FROM contributors;
SELECT contributors.username FROM contributors ORDER BY contributors.username DESC LIMIT 5 OFFSET 5;
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "Contributor99");
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1546394372) ORDER BY revisions.timestamp ASC LIMIT 8 OFFSET 5;
SELECT pages.title FROM pages WHERE (pages.title = "Sport");
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1712262530);
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 15 OFFSET 10;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1728052456);
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1435063619) ORDER BY revisions.timestamp ASC;
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1597207087) ORDER BY revisions.timestamp ASC;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388255423);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1575903527) ORDER BY revisions.timestamp DESC LIMIT 3;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1449505035) ORDER BY revisions.timestamp ASC LIMIT 10;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 15 OFFSET 5;
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1604762251);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1601719199);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388000758);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1568166647) ORDER BY revisions.timestamp DESC LIMIT 3;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "TestUser");
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1486920804);
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "BotFr");
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "WikiEditor42");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1567214868) ORDER BY revisions.timestamp DESC LIMIT 3;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "KhingArthur") AND (revisions.timestamp > 1605049218));
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "AnonymousIP");
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "WikiEditor42") AND (revisions.timestamp > 1395274109));
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1474144535);
SELECT contributors.username FROM contributors ORDER BY contributors.username DESC LIMIT 10 OFFSET 15;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Tour Eiffel");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1520694929) ORDER BY revisions.timestamp DESC LIMIT 5;
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1502365951) ORDER BY revisions.timestamp ASC;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388036190);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Philosophie");
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username FROM contributors;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id ORDER BY revisions.timestamp DESC LIMIT 3;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1555758991) ORDER BY revisions.timestamp DESC LIMIT 10;
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1624596143);
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1689825689);
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1586023177);
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "WikiEditor42");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1597497762) ORDER BY revisions.timestamp DESC LIMIT 20;
SELECT contributors.username FROM contributors ORDER BY contributors.username DESC LIMIT 10 OFFSET 15;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388193796);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT contributors.username FROM contributors;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1387948451);
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors WHERE contributors.username = "WikiEditor42";
SELECT contributors.username FROM contributors WHERE contributors.username = "BotFr";
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388278030);
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "KhingArthur") AND (revisions.timestamp > 1695093877));
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "KhingArthur");
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "AnonymousIP") AND (revisions.timestamp > 1580079169));
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Philosophie");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Sport");
SELECT contributors.username FROM contributors WHERE (contributors.username = "KhingArthur");
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 10 OFFSET 10;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "WikiEditor42") AND (revisions.timestamp > 1758239629));
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1560394287);
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1594108295);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT contributors.username FROM contributors WHERE contributors.username = "AnonymousIP";
SELECT contributors.username FROM contributors WHERE contributors.username = "MPF";
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 15 OFFSET 10;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1558142203);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT contributors.username FROM contributors;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "Z484z");
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "MPF") AND (revisions.timestamp > 1520411037));
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Le scripteur") AND (revisions.timestamp > 1700007388));
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "Robotinator1994");
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1411138813) ORDER BY revisions.timestamp ASC;
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1603271648);
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388152696);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Architecture");
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "WikiEditor42") AND (revisions.timestamp > 1465835609));
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "WikiEditor42") AND (revisions.timestamp > 1550162865));
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1511184149) ORDER BY revisions.timestamp ASC;
SELECT contributors.username FROM contributors WHERE contributors.username = "KhingArthur";
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "Robotinator1994");
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "Contributor99");
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1614513318) ORDER BY revisions.timestamp DESC LIMIT 10;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "TestUser");
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388145882);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1413999211) ORDER BY revisions.timestamp DESC LIMIT 15;
SELECT contributors.username FROM contributors ORDER BY contributors.username DESC LIMIT 20 OFFSET 5;
SELECT contributors.username FROM contributors WHERE (contributors.username = "WikiEditor42");
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "AnonymousIP") AND (revisions.timestamp > 1590750450));
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388043239);
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1710481058) ORDER BY revisions.timestamp ASC;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT contributors.username FROM contributors WHERE contributors.username = "MPF";
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT contributors.username FROM contributors;
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 15;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388207037);
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1542083947) ORDER BY revisions.timestamp ASC LIMIT 10 OFFSET 5;
SELECT contributors.username FROM contributors WHERE (contributors.username = "KhingArthur");
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1672616749);
SELECT contributors.username FROM contributors;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors ORDER BY contributors.username DESC LIMIT 15;
SELECT contributors.username FROM contributors WHERE contributors.username = "MPF";
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Le scripteur") AND (revisions.timestamp > 1627194060));
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors;
SELECT contributors.username FROM contributors WHERE (contributors.username = "BotFr");
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Paris");
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1731876959) ORDER BY revisions.timestamp DESC LIMIT 15;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Philosophie");
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 5;
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "Z484z");
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "KhingArthur") AND (revisions.timestamp > 1679501514));
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Mathématiques");
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1530825480);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "France");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id ORDER BY revisions.timestamp DESC LIMIT 3 OFFSET 3;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1630222740) ORDER BY revisions.timestamp ASC LIMIT 5 OFFSET 3;
SELECT contributors.username FROM contributors;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1585204406);
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 15 OFFSET 10;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1551807706);
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "BotFr") AND (revisions.timestamp > 1737400618));
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1398258230);
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "KhingArthur") AND (revisions.timestamp > 1507910024));
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "MPF") AND (revisions.timestamp > 1684076953));
SELECT contributors.username FROM contributors ORDER BY contributors.username DESC LIMIT 15 OFFSET 10;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1459528580);
SELECT contributors.username FROM contributors WHERE contributors.username = "AnonymousIP";
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Z484z") AND (revisions.timestamp > 1606574043));
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "BotFr");
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1458742700);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "France");
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1669539690) ORDER BY revisions.timestamp ASC;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1758717051) ORDER BY revisions.timestamp DESC LIMIT 5;
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1484480185) ORDER BY revisions.timestamp ASC;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1705653784);
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT pages.title FROM pages WHERE (pages.title = "didine");
SELECT pages.title FROM pages WHERE (pages.title = "Informatique");
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Robotinator1994") AND (revisions.timestamp > 1679377299));
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Chimie");
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "Robotinator1994");
SELECT contributors.username FROM contributors WHERE contributors.username = "Robotinator1994";
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388168138);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id ORDER BY revisions.timestamp DESC LIMIT 5 OFFSET 3;
SELECT contributors.username FROM contributors WHERE (contributors.username = "Le scripteur");
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1753004746) ORDER BY revisions.timestamp DESC LIMIT 20;
SELECT contributors.username FROM contributors WHERE (contributors.username = "BotFr");
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "TestUser") AND (revisions.timestamp > 1705746909));
SELECT pages.title FROM pages WHERE (pages.title = "Mathématiques");
SELECT pages.title FROM pages WHERE (pages.title = "Paris");
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1444782016);
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Robotinator1994") AND (revisions.timestamp > 1675824491));
SELECT contributors.username FROM contributors WHERE contributors.username = "TestUser";
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title FROM pages WHERE (pages.title = "Musique");
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1626981546);
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1452520146);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Paris");
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "WikiEditor42") AND (revisions.timestamp > 1415432428));
SELECT contributors.username FROM contributors ORDER BY contributors.username DESC LIMIT 5 OFFSET 5;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Sport");
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "BotFr");
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388196255);
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "TestUser");
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "AnonymousIP") AND (revisions.timestamp > 1568735600));
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1557741678) ORDER BY revisions.timestamp ASC;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388138987);
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1448424647) ORDER BY revisions.timestamp ASC;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id ORDER BY revisions.timestamp DESC LIMIT 8 OFFSET 3;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388221200);
SELECT contributors.username FROM contributors WHERE (contributors.username = "Le scripteur");
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 5;
SELECT contributors.username FROM contributors WHERE (contributors.username = "Contributor99");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Linguistique");
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388010998);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Sport");
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1569387663) ORDER BY revisions.timestamp ASC;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388250418);
SELECT contributors.username FROM contributors;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1711880760) ORDER BY revisions.timestamp DESC LIMIT 10;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Philosophie");
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1726515230);
SELECT contributors.username FROM contributors WHERE (contributors.username = "MPF");
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Le scripteur") AND (revisions.timestamp > 1578649333));
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1686728261);
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1676890295);
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username FROM contributors;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username FROM contributors;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Linguistique");
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1743990139);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Histoire");
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1423148026) ORDER BY revisions.timestamp DESC LIMIT 10;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1644680488);
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username FROM contributors WHERE contributors.username = "AnonymousIP";
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1634131787);
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1400845865) ORDER BY revisions.timestamp ASC;
SELECT contributors.username FROM contributors WHERE contributors.username = "BotFr";
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username FROM contributors;
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT contributors.username FROM contributors;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 10 OFFSET 5;
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1620196855) ORDER BY revisions.timestamp DESC LIMIT 10;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors WHERE (contributors.username = "TestUser");
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1641897046);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1545433161) ORDER BY revisions.timestamp DESC LIMIT 20;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "WikiEditor42") AND (revisions.timestamp > 1554796970));
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 20 OFFSET 5;
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1669487333);
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "AnonymousIP") AND (revisions.timestamp > 1684503730));
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "Contributor99");
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 20 OFFSET 15;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388273482);
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT pages.title FROM pages WHERE (pages.title = "Tour Eiffel");
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1631598785) ORDER BY revisions.timestamp DESC LIMIT 8;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "KhingArthur") AND (revisions.timestamp > 1671875860));
SELECT contributors.username FROM contributors WHERE contributors.username = "TestUser";
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id ORDER BY revisions.timestamp DESC LIMIT 8 OFFSET 5;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 5;
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1581740996) ORDER BY revisions.timestamp ASC LIMIT 3;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 10 OFFSET 10;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1695936549) ORDER BY revisions.timestamp DESC LIMIT 20;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "BotFr") AND (revisions.timestamp > 1462186098));
SELECT contributors.username FROM contributors;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Mathématiques");
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 20 OFFSET 10;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1687312011) ORDER BY revisions.timestamp DESC LIMIT 3;
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username FROM contributors ORDER BY contributors.username DESC LIMIT 10 OFFSET 5;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Le scripteur") AND (revisions.timestamp > 1651807899));
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 5;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Physique");
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388002916);
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 5 OFFSET 10;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors WHERE contributors.username = "Le scripteur";
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1539187010);
SELECT pages.title FROM pages WHERE (pages.title = "Sport");
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1685806723);
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "Robotinator1994");
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1485222903);
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1606254914);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Robotinator1994") AND (revisions.timestamp > 1552405502));
SELECT contributors.username FROM contributors WHERE contributors.username = "BotFr";
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1394869464) ORDER BY revisions.timestamp ASC LIMIT 5 OFFSET 3;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "MPF");
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 15;
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "AnonymousIP") AND (revisions.timestamp > 1442392971));
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "MPF") AND (revisions.timestamp > 1463536048));
SELECT contributors.username FROM contributors;
SELECT pages.title FROM pages WHERE (pages.title = "Linguistique");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 10;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username FROM contributors;
SELECT contributors.username FROM contributors ORDER BY contributors.username DESC LIMIT 5;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title FROM pages WHERE (pages.title = "France");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1391764869) ORDER BY revisions.timestamp ASC LIMIT 8 OFFSET 5;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id ORDER BY revisions.timestamp DESC LIMIT 3 OFFSET 5;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "BotFr") AND (revisions.timestamp > 1578587399));
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "AnonymousIP");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1387570997);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1590668425) ORDER BY revisions.timestamp DESC LIMIT 3;
SELECT pages.title FROM pages WHERE (pages.title = "Linguistique");
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1397067546);
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 5 OFFSET 10;
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1432125709);
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "MPF") AND (revisions.timestamp > 1482753013));
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 10;
SELECT pages.title FROM pages WHERE (pages.title = "didine");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id ORDER BY revisions.timestamp DESC LIMIT 10 OFFSET 3;
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388173800);
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1498482732) ORDER BY revisions.timestamp DESC LIMIT 3;
SELECT contributors.username FROM contributors WHERE (contributors.username = "MPF");
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Contributor99") AND (revisions.timestamp > 1567323765));
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1749827338) ORDER BY revisions.timestamp DESC LIMIT 3;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "WikiEditor42");
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "TestUser");
SELECT contributors.username FROM contributors WHERE (contributors.username = "MPF");
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "BotFr") AND (revisions.timestamp > 1592184993));
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "TestUser") AND (revisions.timestamp > 1447446264));
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1389118254);
SELECT contributors.username FROM contributors WHERE contributors.username = "AnonymousIP";
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "Z484z");
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "MPF");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT contributors.username FROM contributors WHERE contributors.username = "BotFr";
SELECT contributors.username, AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY revisions.timestamp;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1643963093) ORDER BY revisions.timestamp ASC LIMIT 5 OFFSET 3;
SELECT COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "Contributor99") AND (revisions.timestamp > 1549394820));
SELECT pages.title, revisions.timestamp, contributors.username FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 15;
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "AnonymousIP");
SELECT COUNT(contributors.id) FROM contributors WHERE (contributors.username = "WikiEditor42");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1609489414);
SELECT contributors.username FROM contributors;
SELECT pages.title FROM pages WHERE (pages.title = "Linguistique");
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1387561089);
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1736792995);
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 5 OFFSET 10;
SELECT pages.title FROM pages WHERE (pages.title = "Philosophie");
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors WHERE (contributors.username = "Robotinator1994");
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1406997756);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT MIN(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp < 1388172921);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1394625628) ORDER BY revisions.timestamp ASC LIMIT 5;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Architecture");
SELECT contributors.username FROM contributors WHERE contributors.username = "KhingArthur";
SELECT contributors.username FROM contributors WHERE contributors.username = "KhingArthur";
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id;
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1649277969) ORDER BY revisions.timestamp ASC;
SELECT contributors.username FROM contributors WHERE contributors.username = "TestUser";
SELECT contributors.username FROM contributors ORDER BY contributors.username ASC LIMIT 10 OFFSET 10;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "Le scripteur");
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 10;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "MPF") AND (revisions.timestamp > 1494344980));
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "KhingArthur");
SELECT pages.title FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1624059495) ORDER BY revisions.timestamp ASC;
SELECT AVG(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE ((contributors.username = "TestUser") AND (revisions.timestamp > 1419948836));
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id;
SELECT contributors.username FROM contributors JOIN revisions ON revisions.contributor_id = contributors.id WHERE (revisions.timestamp > 1571453734);
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1640134523) ORDER BY revisions.timestamp DESC LIMIT 8;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (pages.title = "Philosophie");
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1700248131);
SELECT contributors.username FROM contributors WHERE contributors.username = "Le scripteur";
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1705940308);
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT contributors.username FROM contributors WHERE contributors.username = "WikiEditor42";
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username FROM contributors ORDER BY contributors.username DESC LIMIT 15;
SELECT pages.title FROM pages WHERE (pages.title = "Informatique");
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1544483098);
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username;
SELECT pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1700832277) ORDER BY revisions.timestamp ASC LIMIT 10 OFFSET 3;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT MAX(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1721858373);
SELECT contributors.username, pages.title, revisions.timestamp FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id WHERE (contributors.username = "KhingArthur");
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1419024850);
SELECT SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id WHERE (revisions.timestamp > 1679971072);
SELECT pages.title FROM pages WHERE (pages.title = "Histoire");
SELECT contributors.username FROM contributors WHERE (contributors.username = "MPF");
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 10 OFFSET 10;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 10 OFFSET 10;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username;
SELECT contributors.username, SUM(revisions.timestamp) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username LIMIT 10 OFFSET 5;
SELECT contributors.username FROM contributors;
SELECT contributors.username, COUNT(pages.id) FROM pages JOIN revisions ON pages.revision_id = revisions.id JOIN contributors ON revisions.contributor_id = contributors.id GROUP BY contributors.username ORDER BY contributors.username ASC;
